name: Atualizar README

on:
  schedule:
    - cron: '0 * * * *'
  workflow_dispatch:

jobs:
  update-readme:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Clonar repositório
        uses: actions/checkout@v3

      - name: Atualizar README
        run: |
          echo "🕒 Atualizado em $(date '+%d/%m/%Y %H:%M:%S')" > update.txt
          cat update.txt >> README.md
          git config --global user.name 'nailsonlima'
          git config --global user.email '78036929+nailsonlima@users.noreply.github.com'
          git add README.md
          git commit -m "update automático no README" || echo "Nenhuma mudança pra commitar"
          git push https://x-access-token:${{ secrets.GITHUB_TOKEN }}@github.com/${{ github.repository }} HEAD:main
